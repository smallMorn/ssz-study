spring:
  application:
    name: ssz-service-user
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true

server:
  port: 1002
#开启feign对sentinel的支持
feign:
  sentinel:
    enabled: true