spring:
  application:
    name: ssz-service-user
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: "nacos"
        password: "nacos"
      config:
        server-addr: 127.0.0.1:8848
        username: "nacos"
        password: "nacos"
        file-extension: yml
server:
  port: 1002
#开启feign对sentinel的支持
feign:
  sentinel:
    enabled: true